name: Keep Render App Awake (ping)

on:
  schedule:
    - cron: "*/10 * * * *"  # 10분마다 실행
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send ping to Render app
        run: curl -s https://cafembtitest.onrender.com/ > /dev/null
